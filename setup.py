from setuptools import setup

setup(name='hybridimg',
      version = '0.1',
      description = "A package which can be used to create hybrid image from two input images",
      packages = ['hybridimg'],
      author = ['Brian Pinto'],
      license = 'The MIT License',
      zip_safe = False)
